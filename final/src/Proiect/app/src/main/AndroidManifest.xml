<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mihaela.proiectmihaela">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".VizualizareTesteActivity" />
        <activity android:name=".GestionareTesteActivity" />
        <activity android:name=".ProfilActivity" />
        <activity android:name=".RapoarteActivity" />
        <activity android:name=".LogareActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".InregistreareActivity" />
        <activity android:name=".RecuperareParolaActivity" />
        <activity android:name=".FeedBackActivity" />
        <activity android:name=".InscriereTestActivity" />
        <activity android:name=".TestActivity" />
        <activity android:name=".RezultatTestActivity" />
        <activity android:name=".CreareTestActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".ProfilProfesorActivity">
        </activity>
    </application>

</manifest>